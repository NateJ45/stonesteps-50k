---
// Foundation, edit with care
// Renders a page-builder section array. Maps each block _type to its component
// and OWNS the alternating background cadence for content blocks, so reordering
// can never break the page rhythm (that is why blocks have no color field).
// Self-contained blocks (hero, CTA band, stats, spacer) manage their own surface.
//
// Cadence logic lives in src/lib/sectionCadence.ts and is unit-tested separately.
// This file is thin template glue: it imports classified rows and delegates.
//
// The GROQ query that loads `sections` must project images with imageProjection
// and ctaBlocks with ctaProjection (see sectionsProjection() in queries.ts).
import Hero from '@/components/Hero.astro';
import FinalCta from '@/components/FinalCta.astro';
import StatsRow from '@/components/StatsRow.astro';
import SectionDivider from '@/components/SectionDivider.astro';
import RichTextSection from '@/components/sections/RichTextSection.astro';
import ImageText from '@/components/sections/ImageText.astro';
import GalleryGrid from '@/components/sections/GalleryGrid.astro';
import QuoteBlock from '@/components/sections/QuoteBlock.astro';
import VideoEmbed from '@/components/sections/VideoEmbed.astro';
import FounderSection from '@/components/sections/FounderSection.astro';
import ServicesGridSection from '@/components/sections/ServicesGridSection.astro';
import TestimonialsSection from '@/components/sections/TestimonialsSection.astro';
import StorySection from '@/components/sections/StorySection.astro';
import ValuesSection from '@/components/sections/ValuesSection.astro';
import ProcessSection from '@/components/sections/ProcessSection.astro';
import ServiceAreaSection from '@/components/sections/ServiceAreaSection.astro';
import GuaranteeSection from '@/components/sections/GuaranteeSection.astro';
import FaqSection from '@/components/sections/FaqSection.astro';
import LogoStrip from '@/components/sections/LogoStrip.astro';
import TeamGrid from '@/components/sections/TeamGrid.astro';
import EmbedSection from '@/components/sections/EmbedSection.astro';
import DynamicList from '@/components/sections/DynamicList.astro';
// Stone Steps race blocks.
import RaceHero from '@/components/sections/RaceHero.astro';
import DistanceTickets from '@/components/sections/DistanceTickets.astro';
import RecordsBoard from '@/components/sections/RecordsBoard.astro';
import RaceSchedule from '@/components/sections/RaceSchedule.astro';
import PhotoBand from '@/components/sections/PhotoBand.astro';
import CourseFeatures from '@/components/sections/CourseFeatures.astro';
import LoopCard from '@/components/sections/LoopCard.astro';
import ElevationBand from '@/components/sections/ElevationBand.astro';
import CourseMapBand from '@/components/sections/CourseMapBand.astro';
import SponsorPatches from '@/components/sections/SponsorPatches.astro';
import PageHeader from '@/components/sections/PageHeader.astro';
import ContactBand from '@/components/sections/ContactBand.astro';
import FaqKiosk from '@/components/sections/FaqKiosk.astro';
import Ticker from '@/components/sections/Ticker.astro';
import ParksBand from '@/components/sections/ParksBand.astro';
import CommunityBand from '@/components/sections/CommunityBand.astro';
import RaceWeather from '@/components/sections/RaceWeather.astro';
import Dynasties from '@/components/sections/Dynasties.astro';
import GearBand from '@/components/sections/GearBand.astro';
import { classifySections } from '@/lib/sectionCadence';
import type {
  PageBuilderBlock,
  ProjectedRaceResult,
  ProjectedRecordEntry,
} from '@/lib/pageBuilder.types';
import { sectionEditAttr, type EditDoc } from '@/lib/preview-edit-attr';

interface Props {
  sections?: unknown[];
  /**
   * The results archive, fetched ONCE per page rather than once per section.
   * The records board and the dynasties band both need every finish on file;
   * projected inside `pageBuilder[]` that scan ran per section and cost six
   * seconds on the home page. See ARCHIVE_PROJECTION in src/lib/queries.ts.
   * A section's own value still wins, so nothing that already passes its own
   * results changes behaviour.
   */
  results?: ProjectedRaceResult[];
  historical?: ProjectedRecordEntry[];
  /** Prefix for generated heading ids, so two renderers on a page do not collide. */
  idPrefix?: string;
  /**
   * PREVIEW ONLY. When the Studio preview route passes the document each
   * section belongs to, every section is wrapped in a real block box carrying a
   * `data-sanity` attribute, which is what gives the Presentation overlay its
   * in-canvas insert / duplicate / remove / drag-to-reorder controls
   * (PORTS.md card 17).
   *
   * The live pages never pass it, and with it absent the wrapper is a
   * `<Fragment>`, which renders NOTHING at all. That is why the static build's
   * markup stays byte-identical with this feature installed, and
   * `npm run parity compare` is the standing gate on that promise.
   */
  editDoc?: EditDoc;
}

const {
  sections = [],
  idPrefix = 'section',
  editDoc,
  results: sharedResults = [],
  historical: sharedHistorical = [],
} = Astro.props as Props;
const rows = classifySections<PageBuilderBlock>(sections as PageBuilderBlock[], idPrefix);

// A real element in preview, nothing at all on the live site. The wrapper must
// NOT be `display: contents`: the overlay outlines the element's rect, and a
// contents element has none. A plain block-level div around a full-width
// <section> changes no layout.
const Wrap = editDoc ? 'div' : Fragment;
const wrapAttrs = (key: string | undefined) =>
  editDoc && key ? { 'data-sanity': sectionEditAttr(editDoc, key) } : {};
---

{
  rows.map(({ block: s, surface, headingId }) => (
    <>
      <Wrap {...wrapAttrs((s as { _key?: string })._key)}>
        {s._type === 'heroSection' ? (
          <Hero
            eyebrow={s.eyebrow}
            headline={s.headline ?? ''}
            subhead={s.subhead}
            backgroundImage={s.backgroundImage}
            primaryCta={s.primaryCta}
            secondaryCta={s.secondaryCta}
            scriptAccent={s.scriptAccent}
            size={s.size ?? 'short'}
            headingId={headingId}
          />
        ) : s._type === 'ctaBandSection' ? (
          <FinalCta
            eyebrow={s.eyebrow}
            headline={s.headline}
            subhead={s.subhead}
            subheadRich={s.subheadRich}
            cta={s.cta}
            scriptAccent={s.scriptAccent}
            headingAccent={s.headingAccent}
            backgroundImage={s.backgroundImage}
            headingId={headingId}
          />
        ) : s._type === 'statSection' ? (
          <StatsRow
            stats={
              (s.stats ?? []).filter((x) => typeof x?.number === 'number') as {
                number: number;
                suffix?: string;
                label: string;
              }[]
            }
          />
        ) : s._type === 'spacerSection' ? (
          s.variant === 'space' ? (
            <div class="py-section-md" aria-hidden="true" />
          ) : (
            <SectionDivider variant={s.variant === 'line' ? 'line' : 'ornament'} />
          )
        ) : s._type === 'richTextSection' ? (
          <RichTextSection
            eyebrow={s.eyebrow}
            heading={s.heading}
            scriptAccent={s.scriptAccent}
            headingAccent={s.headingAccent}
            body={s.body}
            width={s.width}
            align={s.align}
            surface={surface ?? 'background'}
            headingId={headingId}
          />
        ) : s._type === 'imageTextSection' ? (
          <ImageText
            image={s.image}
            imageSide={s.imageSide}
            cutout={s.cutout}
            eyebrow={s.eyebrow}
            heading={s.heading}
            body={s.body}
            cta={s.cta}
            surface={surface ?? 'background'}
            headingId={headingId}
          />
        ) : s._type === 'gallerySection' ? (
          <GalleryGrid
            heading={s.heading}
            images={s.images}
            columns={s.columns}
            surface={surface ?? 'background'}
            headingId={headingId}
          />
        ) : s._type === 'quoteSection' ? (
          <QuoteBlock
            quote={s.quote}
            attribution={s.attribution}
            detail={s.detail}
            surface={surface ?? 'background'}
          />
        ) : s._type === 'videoSection' ? (
          <VideoEmbed
            url={s.url}
            heading={s.heading}
            caption={s.caption}
            surface={surface ?? 'background'}
            headingId={headingId}
          />
        ) : s._type === 'founderSection' ? (
          <FounderSection
            portrait={s.portrait}
            eyebrow={s.eyebrow}
            headline={s.headline}
            content={s.content}
            cta={s.cta}
            headingId={headingId}
          />
        ) : s._type === 'servicesGridSection' ? (
          <ServicesGridSection
            eyebrow={s.eyebrow}
            headline={s.headline}
            scriptAccent={s.scriptAccent}
            headingAccent={s.headingAccent}
            subhead={s.subhead}
            subheadRich={s.subheadRich}
            services={s.services ?? []}
            cta={s.cta}
            footnote={s.footnote}
            variant={s.variant ?? 'grid'}
            headingId={headingId}
          />
        ) : s._type === 'testimonialsSection' ? (
          <TestimonialsSection
            eyebrow={s.eyebrow}
            headline={s.headline}
            scriptAccent={s.scriptAccent}
            headingAccent={s.headingAccent}
            subhead={s.subhead}
            subheadRich={s.subheadRich}
            featuredQuote={s.featuredQuote}
            testimonialsToShow={s.testimonialsToShow ?? []}
            attribution={s.attribution}
            headingId={headingId}
          />
        ) : s._type === 'storySection' ? (
          <StorySection
            eyebrow={s.eyebrow}
            headline={s.headline}
            content={s.content}
            portrait={s.portrait}
            attribution={s.attribution}
            credentialLine={s.credentialLine}
            serviceAreaLine={s.serviceAreaLine}
            surface={surface ?? 'background'}
            headingId={headingId}
          />
        ) : s._type === 'valuesSection' ? (
          <ValuesSection
            eyebrow={s.eyebrow}
            headline={s.headline}
            points={s.points ?? []}
            columns={s.columns}
            headingId={headingId}
          />
        ) : s._type === 'processSection' ? (
          <ProcessSection
            eyebrow={s.eyebrow}
            headline={s.headline}
            subhead={s.subhead}
            steps={s.steps ?? []}
            variant={s.variant ?? 'preview'}
            cta={s.cta}
            headingId={headingId}
          />
        ) : s._type === 'serviceAreaSection' ? (
          <ServiceAreaSection
            eyebrow={s.eyebrow}
            headline={s.headline}
            description={s.description}
            travelFees={s.travelFees ?? []}
            showTravelFees={s.showTravelFees ?? true}
            surface={surface ?? 'background'}
            headingId={headingId}
          />
        ) : s._type === 'guaranteeSection' ? (
          <GuaranteeSection
            text={s.text}
            siteSettingsText={s.siteSettingsText}
            surface={surface ?? 'background'}
          />
        ) : s._type === 'faqSection' ? (
          <FaqSection
            eyebrow={s.eyebrow}
            headline={s.headline}
            headingAccent={s.headingAccent}
            subhead={s.subhead}
            subheadRich={s.subheadRich}
            items={s.items ?? []}
            cta={s.cta}
            headingId={headingId}
          />
        ) : s._type === 'logoStripSection' ? (
          <LogoStrip
            eyebrow={s.eyebrow}
            headline={s.headline}
            logos={s.logos ?? []}
            layout={s.layout ?? 'row'}
            headingId={headingId}
          />
        ) : s._type === 'teamSection' ? (
          <TeamGrid
            eyebrow={s.eyebrow}
            headline={s.headline}
            subhead={s.subhead}
            subheadRich={s.subheadRich}
            members={s.members ?? []}
            headingId={headingId}
          />
        ) : s._type === 'embedSection' ? (
          <EmbedSection
            eyebrow={s.eyebrow}
            headline={s.headline}
            subhead={s.subhead}
            embedUrl={s.embedUrl}
            embedCode={s.embedCode}
            heightHint={s.heightHint ?? 'medium'}
            headingId={headingId}
          />
        ) : s._type === 'dynamicListSection' ? (
          <DynamicList
            eyebrow={s.eyebrow}
            headline={s.headline}
            subhead={s.subhead}
            subheadRich={s.subheadRich}
            source={s.source}
            items={s.items ?? []}
            cta={s.cta}
            columns={s.columns}
            limit={s.limit}
            headingId={headingId}
          />
        ) : s._type === 'raceHeroSection' ? (
          <RaceHero
            eyebrow={s.eyebrow}
            headline={s.headline}
            subhead={s.subhead}
            claim={s.claim}
            images={s.images}
            image={s.image}
            showCountdown={s.showCountdown}
            primaryCta={s.primaryCta}
            secondaryCta={s.secondaryCta}
            race={s.race}
            headingId={headingId}
          />
        ) : s._type === 'distanceTicketsSection' ? (
          <DistanceTickets
            eyebrow={s.eyebrow}
            headline={s.headline}
            note={s.note}
            distances={s.distances ?? []}
            race={s.race}
            headingId={headingId}
          />
        ) : s._type === 'recordsBoardSection' ? (
          <RecordsBoard
            eyebrow={s.eyebrow}
            headline={s.headline}
            intro={s.intro}
            distances={s.distances ?? []}
            results={s.results ?? sharedResults}
            historical={s.historical ?? sharedHistorical}
            race={s.race}
            surface={surface ?? 'background'}
            headingId={headingId}
          />
        ) : s._type === 'photoBandSection' ? (
          <PhotoBand image={s.image} caption={s.caption} height={s.height} />
        ) : s._type === 'raceScheduleSection' ? (
          <RaceSchedule
            eyebrow={s.eyebrow}
            headline={s.headline}
            items={s.items ?? []}
            headingId={headingId}
            race={s.race}
          />
        ) : s._type === 'courseFeaturesSection' ? (
          <CourseFeatures
            eyebrow={s.eyebrow}
            headline={s.headline}
            cta={s.cta}
            image={s.image}
            caption={s.caption}
            features={s.features ?? []}
            surface={surface ?? 'background'}
            headingId={headingId}
          />
        ) : s._type === 'loopCardSection' ? (
          <LoopCard
            eyebrow={s.eyebrow}
            headline={s.headline}
            legend={s.legend}
            sourceNote={s.sourceNote}
            loops={s.loops ?? []}
            aside={s.aside ?? []}
            headingId={headingId}
          />
        ) : s._type === 'elevationSection' ? (
          <ElevationBand
            eyebrow={s.eyebrow}
            headline={s.headline}
            intro={s.intro}
            totalGain={s.totalGain}
            race={s.race}
            surface={surface ?? 'background'}
            headingId={headingId}
          />
        ) : s._type === 'courseMapSection' ? (
          <CourseMapBand
            eyebrow={s.eyebrow}
            headline={s.headline}
            intro={s.intro}
            surface={surface ?? 'background'}
            headingId={headingId}
          />
        ) : s._type === 'sponsorPatchesSection' ? (
          <SponsorPatches
            eyebrow={s.eyebrow}
            headline={s.headline}
            sponsors={s.sponsors ?? []}
            headingId={headingId}
          />
        ) : s._type === 'pageHeaderSection' ? (
          <PageHeader
            eyebrow={s.eyebrow}
            headline={s.headline}
            headlineSecondLine={s.headlineSecondLine}
            lede={s.lede}
            image={s.image}
            cutout={s.cutout}
            headingId={headingId}
          />
        ) : s._type === 'contactSection' ? (
          <ContactBand
            eyebrow={s.eyebrow}
            headline={s.headline}
            subjects={s.subjects ?? []}
            communityNote={s.communityNote}
            race={s.race}
            surface={surface ?? 'background'}
            headingId={headingId}
          />
        ) : s._type === 'faqKioskSection' ? (
          <FaqKiosk
            eyebrow={s.eyebrow}
            headline={s.headline}
            items={s.items ?? []}
            unansweredHeading={s.unansweredHeading}
            unansweredNote={s.unansweredNote}
            unanswered={s.unanswered ?? []}
            distances={s.distances ?? []}
            headingId={headingId}
          />
        ) : s._type === 'tickerSection' ? (
          <Ticker items={s.items ?? []} />
        ) : s._type === 'raceWeatherSection' ? (
          <RaceWeather
            eyebrow={s.eyebrow}
            headline={s.headline}
            intro={s.intro}
            surface={surface ?? 'background'}
            headingId={headingId}
          />
        ) : s._type === 'parksSection' ? (
          <ParksBand
            eyebrow={s.eyebrow}
            headline={s.headline}
            body={s.body}
            image={s.image}
            caption={s.caption}
            showDirector={s.showDirector}
            cta={s.cta}
            race={s.race}
            headingId={headingId}
          />
        ) : s._type === 'communitySection' ? (
          <CommunityBand
            eyebrow={s.eyebrow}
            headline={s.headline}
            body={s.body}
            points={s.points ?? []}
            cta={s.cta}
            race={s.race}
            surface={surface ?? 'background'}
            headingId={headingId}
          />
        ) : s._type === 'dynastiesSection' ? (
          <Dynasties
            eyebrow={s.eyebrow}
            headline={s.headline}
            cta={s.cta}
            distances={s.distances ?? []}
            results={s.results ?? sharedResults}
            historical={s.historical ?? sharedHistorical}
            surface={surface ?? 'background'}
            headingId={headingId}
          />
        ) : s._type === 'gearSection' ? (
          <GearBand
            eyebrow={s.eyebrow}
            headline={s.headline}
            items={s.items ?? []}
            surface={surface ?? 'background'}
            headingId={headingId}
          />
        ) : (
          // Unknown block type: render nothing, log in dev.
          import.meta.env.DEV &&
          console.warn(
            // `s` is `never` here: every member of PageBuilderBlock is handled
            // above, which is exactly what this branch exists to prove. The cast
            // is only so the runtime warning can name the type Sanity actually
            // sent when a NEW block ships without a mapping. Written as a
            // Record rather than an inline object type on purpose: a brace
            // inside an .astro template expression ends that expression, even
            // inside a comment, so an inline object type breaks the parse.
            `[SectionRenderer] Unknown block type: "${(s as Record<string, unknown>)._type}". Add a mapping in SectionRenderer.astro.`,
          )
        )}
      </Wrap>
    </>
  ))
}
