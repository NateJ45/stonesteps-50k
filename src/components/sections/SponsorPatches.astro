---
// Foundation, edit with care
//
// Sponsors, as embroidered patches.
//
// SELF_CONTAINED: paints its own ground, never receives a surface prop.
//
// This replaced the greyscale-logos-at-70%-opacity strip, which is the single
// most common generated-design default in existence. Logos show in FULL COLOUR
// here, because patches are colourful and because a sponsor who paid deserves
// their actual mark rather than a washed-out one.

import SanityImage from '@/components/SanityImage.astro';
import type { ProjectedSponsorPatchesSection } from '@/lib/pageBuilder.types';

type Props = {
  eyebrow?: string;
  headline?: string;
  sponsors?: ProjectedSponsorPatchesSection['sponsors'];
  headingId?: string;
};

const {
  eyebrow,
  headline = 'With thanks',
  sponsors = [],
  headingId = 'sponsors-heading',
} = Astro.props as Props;
---

{
  sponsors.length > 0 && (
    <section class="bg-background py-section-md" aria-labelledby={headingId}>
      <div class="mx-auto w-full max-w-content px-6">
        {eyebrow && (
          <p class="blaze text-heading-accent mb-4 font-mono text-[0.72rem] font-semibold tracking-[0.22em] uppercase">
            {eyebrow}
          </p>
        )}
        <h2 id={headingId} class="display text-h3">
          {headline}
        </h2>

        <ul class="mt-8 flex flex-wrap items-stretch gap-6">
          {sponsors.map((s) => (
            <li class="patch min-h-[7.75rem] w-full max-w-[13rem] px-5 py-6">
              {s.logo?.asset ? (
                <SanityImage
                  source={s.logo}
                  width={320}
                  sizes="208px"
                  alt={s.logo.alt ?? s.name ?? ''}
                  class="max-h-[4.4rem] w-auto object-contain"
                />
              ) : (
                <span class="display on-light text-center text-[1.05rem] text-[color:var(--plate-ink)]">
                  {s.name}
                </span>
              )}
            </li>
          ))}
        </ul>
      </div>
    </section>
  )
}
